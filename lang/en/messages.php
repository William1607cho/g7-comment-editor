<?php

/**
 * Server-side translations (PHP __()).
 *
 * This plugin has no server code (pure global JS), so nothing in the backend
 * references these keys. The file exists only to satisfy the core extension
 * language-path convention (plugins/{id}/lang/{locale}/*.php). Front-end strings
 * live in resources/lang/{locale}.json.
 */
return [
    'name' => 'Comment Editor',
];
